{
  "name": "hillel-auto-cypress",
  "version": "1.0.2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:run:cloud": "npx cypress run --record --key ${RECORD_KEY} --parallel",
    "cy:run": "npx cypress run",
    "cy:run:chrome": "npx cypress run --browser chrome",
    "cy:run:firefox": "npx cypress run --browser firefox",
    "cy:open": "npx cypress open"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.6.1",
    "eslint-plugin-cypress": "^2.15.1"
  }
}
